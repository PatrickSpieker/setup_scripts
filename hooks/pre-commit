#!/usr/bin/env bash
# Shared pre-commit hook: runs the lint suite via test_runner.sh if present.
# Repos without a test_runner.sh are silently skipped.
set -euo pipefail
REPO_DIR="$(git rev-parse --show-toplevel)"
if [[ -x "$REPO_DIR/test_runner.sh" ]]; then
  "$REPO_DIR/test_runner.sh" lint
fi
